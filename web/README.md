# Playable prototype

This is the first mobile-first PWA prototype. It runs entirely in the browser using clearly labelled fictional demo data.

## Test on iPhone

The simplest route is to serve the repository with a static host such as GitHub Pages. The web root is the `web/` folder.

This prototype is intentionally separate from the production player database. The next phase will connect the UI to the quiz API and real licensed player data.
